name: My first github workflow
on:   
  push:
    branches: [ master ]
env:
  GITHUB_TOKEN: Github

jobs:
  github_checkout_stage:
    runs-on: ubuntu-latest
    steps:
    - name: checkout_code
      id: checkout
      uses: actions/checkout@v1